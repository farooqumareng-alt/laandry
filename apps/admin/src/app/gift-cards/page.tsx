import { PlaceholderPage } from '@/components/placeholder-page';

export default function GiftCardsAdminPage() {
  return (
    <PlaceholderPage
      kicker="Admin · /gift-cards"
      title="Gift Cards"
      description="Gift card issuance, balance ledger, and fraud controls. Deferred to Phase 11 per the MVP boundary (docs/ARCHITECTURE.md §17)."
      emptyState="Not built yet — Phase 11."
    />
  );
}
