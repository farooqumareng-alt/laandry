import { PlaceholderPage } from '@/components/placeholder-page';

export default function PromotionsPage() {
  return (
    <PlaceholderPage
      kicker="Admin · /promotions"
      title="Promotions"
      description="Promo codes and automatic promotions with eligibility, usage limits, and redemption tracking. Deferred to Phase 11 per the MVP boundary (docs/ARCHITECTURE.md §17)."
      emptyState="Not built yet — Phase 11."
    />
  );
}
