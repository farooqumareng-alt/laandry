import { PlaceholderPage } from '@/components/placeholder-page';

export default function ReportsPage() {
  return (
    <PlaceholderPage
      kicker="Admin · /reports"
      title="Reports"
      description="Operational and financial reporting — order volume, on-time rates, provider utilization, revenue and payout summaries."
      emptyState="No reports configured yet."
    />
  );
}
