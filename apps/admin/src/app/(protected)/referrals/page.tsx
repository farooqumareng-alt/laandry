import { PlaceholderPage } from '@/components/placeholder-page';

export default function ReferralsAdminPage() {
  return (
    <PlaceholderPage
      kicker="Admin · /referrals"
      title="Referrals"
      description="Referral program configuration and anti-abuse controls. Deferred to Phase 11 per the MVP boundary (docs/ARCHITECTURE.md §17)."
      emptyState="Not built yet — Phase 11."
    />
  );
}
