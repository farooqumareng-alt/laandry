import { PlaceholderPage } from '@/components/placeholder-page';

export default function ProductsPage() {
  return (
    <PlaceholderPage
      kicker="Admin · /products"
      title="Products"
      description="Laandry Store catalog and inventory — detergents, hangers, garment bags, travel kits. Deferred to Phase 12 per the MVP boundary (docs/ARCHITECTURE.md §17)."
      emptyState="Not built yet — Phase 12."
    />
  );
}
