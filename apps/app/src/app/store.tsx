import { Screen } from '@/components/screen';

export default function StoreScreen() {
  return (
    <Screen
      kicker="Customer app · /store"
      title="Laandry Store"
      description="Detergents, hangers, garment bags, travel kits — contextual checkout add-ons and standalone purchase. Deferred to Phase 12 per the MVP boundary."
    />
  );
}
