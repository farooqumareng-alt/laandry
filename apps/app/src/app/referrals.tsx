import { Screen } from '@/components/screen';

export default function ReferralsScreen() {
  return (
    <Screen
      kicker="Customer app · /referrals"
      title="Invite a friend"
      description="Personal referral link — credit is granted only after the referred customer's first completed paid order, never on signup (docs/ARCHITECTURE.md §7). Deferred to Phase 11."
    />
  );
}
